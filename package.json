{
  "name": "counterapi-worker",
  "version": "1.0.0",
  "description": "Self-hosted Cloudflare Worker replacement for CounterAPI",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:production-durability": "node scripts/production-durability-check.mjs",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
